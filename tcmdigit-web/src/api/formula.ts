import type { Formula } from '@/types/formula'

export const getFormulaList = async (): Promise<Formula[]> => {
  // TODO: 替换为实际的 API 请求
  return Promise.resolve([])
}

export const getFormulaDetails = async (id: number): Promise<Formula> => {
  // TODO: 替换为实际的 API 请求
  return Promise.resolve({} as Formula)
}
